# srmip
Python library for Medical Image Processing (MIP).

## Installation and working environment
```
.venv/Scripts/activate
pip install poetry
poetry install (for developer)
poetry install --no-root (for users)
```